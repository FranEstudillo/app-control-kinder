dependencies:
  flutter:
    sdk: flutter
  # ... otras dependencias
  image_picker: ^1.1.2 # O la versión más reciente
name: app_control_kinder_v4
description: "A new Flutter project."
publish_to: "none"
version: 0.1.0

environment:
  sdk: ^3.8.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
